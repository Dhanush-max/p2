/** @type {import('tailwindcss').Config} */
// tailwind.config.js
export default {
  content: [
    "./src/**/*.{html,js,jsx,ts,tsx}", // Adjust this to match your file structure
  ],
  theme: {
    extend: {},
  },
  plugins: [
    // Remove the reference to "rombo-tailwind" as it doesn't exist
    // Add other valid Tailwind plugins here if needed
  ],
};